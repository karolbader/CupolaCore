# EPI Contracts v1

These files are JSON Schema (draft 2020-12) contracts for EPI `v1` payloads.

Demo JSON instances live under:

- `demo_packs/v0/demo_pack/`

Current validation behavior:

- `epi-cli schema-validate` currently checks JSON parsing only.
- Full JSON Schema rule enforcement is not implemented yet.
